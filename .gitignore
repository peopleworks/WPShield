**/bin/
**/obj/
.vs/
.vscode/
.idea/
TestResults/
artifacts/
*.user
*.suo
*.nupkg
*.snupkg
.env
appsettings.Production.json
secrets.json

# Operator-specific configuration. Real hostnames, destinations and topology
# must never reach the public repository; see docs/en/operator-configuration.md.
appsettings.Local.json
appsettings.*.Local.json
DEVELOPMENT_PLAN.local.md

# JSON Lines gateway logs. They carry real hostnames, real paths and real client
# addresses, so a stray commit of one leaks more than any configuration file would.
logs/
*.jsonl
