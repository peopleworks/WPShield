**/bin/
**/obj/
.vs/
.vscode/
.idea/
TestResults/
# Publish output. Self-contained builds are ~106 MB and are produced from source on demand;
# a build in the tree is also the one place a stray appsettings.Local.json could be committed.
artifacts/
*.user
*.suo
*.nupkg
*.snupkg
.env
appsettings.Production.json
secrets.json

# Operator-specific configuration. Real hostnames, destinations and topology
# must never reach the public repository; see docs/en/operator-configuration.md.
appsettings.Local.json
appsettings.*.Local.json
DEVELOPMENT_PLAN.local.md

# JSON Lines gateway logs and triage reports. Both carry real hostnames, real paths
# and real client addresses, so a stray commit of one leaks more than any
# configuration file would. The triage tool writes its report into the current
# directory by default, which for anyone running it from a clone is this one.
logs/
*.jsonl
